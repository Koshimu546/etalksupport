<h1>検索結果</h1>

<% if @users.any? %>
  <ul>
    <% @users.each do |user| %>
      <li>
        <%= link_to user.profile.username, user_path(user) %> <!-- ユーザーネームをリンクに -->
      </li>
    <% end %>
  </ul>
<% else %>
  <p>該当するユーザーが見つかりませんでした。</p>
<% end %>

<%= link_to "マイページに戻る", user_path(current_user) %>
